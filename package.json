{
  "name": "nu-miezz-a-piacer",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "vitest": "^4.1.8"
  }
}
