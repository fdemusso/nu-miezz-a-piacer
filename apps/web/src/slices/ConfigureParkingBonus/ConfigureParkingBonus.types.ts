// Local view-state types for the ConfigureParkingBonus slice.
// Import shared domain types only from '@vsa/contracts'.

export interface ConfigureParkingBonusViewState {
  status: 'idle' | 'loading' | 'error'
}
