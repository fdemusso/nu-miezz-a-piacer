// Local view-state types for the DefineSensitiveZone slice.
// Import shared domain types only from '@vsa/contracts'.

export interface DefineSensitiveZoneViewState {
  status: 'idle' | 'loading' | 'error'
}
