// Local view-state types for the HighDensityZoneMap slice.
// Import shared domain types only from '@vsa/contracts'.

export interface HighDensityZoneMapViewState {
  status: 'idle' | 'loading' | 'error'
}
